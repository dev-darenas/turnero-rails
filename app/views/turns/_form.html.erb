<%= form_with(model: turn, local: true) do |form| %>
  <% if turn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(turn.errors.count, "error") %> prohibited this turn from being saved:</h2>

      <ul>
      <% turn.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :specialty %>
    <%= form.select :specialty, options_for_select(Turn.specialties.map{ |e| [e[0], e[0]] }) %>
  </div>

  <div class="field">
    <%= form.label :cc %>
    <%= form.text_field :cc, id: :turn_cc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
