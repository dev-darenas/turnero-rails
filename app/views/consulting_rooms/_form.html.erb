<%= form_with(model: consulting_room, local: true) do |form| %>
  <% if consulting_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(consulting_room.errors.count, "error") %> prohibited this consulting_room from being saved:</h2>

      <ul>
      <% consulting_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :consulting_room_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
